version: "2"

linters:
  settings:
    errcheck:
      # Terminal output writers: a failed write to stdout/stderr has no
      # meaningful recovery in a CLI. Cleanup-path Close/Remove calls are
      # explicitly discarded with `_ =` in code instead.
      exclude-functions:
        - fmt.Fprintf
        - fmt.Fprintln
        - fmt.Fprint
