name: release

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    permissions:
      contents: write # create the GitHub Release and upload artifacts
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0 # goreleaser needs full history for the changelog

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      # Mirrors ci.yml exactly: unit tests race-instrumented (45m — the
      # §31.13 wasm compile is several-fold slower under race), e2e NOT
      # raced. e2e drives the real binary through its production 20s
      # extraction deadline; race instrumentation pushes a cold child past
      # it on CI runners (observed at the v0.5.0 tag: 22.2s > 20s), which
      # tests the instrumentation, not the product.
      - name: Run tests
        run: |
          go test -race -timeout 45m -count=1 ./...
          go test -count=1 -tags=e2e ./e2e/...

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@f06c13b6b1a9625abc9e6e439d9c05a8f2190e94 # v7.2.3
        with:
          distribution: goreleaser
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HOMEBREW_TAP_GITHUB_TOKEN: ${{ secrets.HOMEBREW_TAP_GITHUB_TOKEN }}
